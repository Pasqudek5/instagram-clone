/*
 * FeedPage Messages
 *
 * This contains all the text for the FeedPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.FeedPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the FeedPage container!',
  },
});
